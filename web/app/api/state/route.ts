import { getChatGPTUser } from '@/app/chatgpt-auth';
import { database } from '@/lib/storage';
import { emptyState,stateSchema } from '@/lib/product';
export const dynamic='force-dynamic';
const reply=(body:unknown,status=200)=>Response.json(body,{status,headers:{'Cache-Control':'private, no-store','X-Content-Type-Options':'nosniff'}});
export async function GET(){try{const user=await getChatGPTUser();if(!user)return reply({error:'Войдите, чтобы открыть свой профиль.'},401);const row=await database().prepare('SELECT data,revision FROM workspaces WHERE user_id = ?').bind(user.userId).first<{data:string,revision:number}>();return reply({state:row?JSON.parse(row.data):emptyState,revision:row?.revision||0})}catch{console.error('JobPilot state read failed');return reply({error:'Не удалось загрузить данные. Попробуйте ещё раз.'},503)}}
export async function PUT(request:Request){try{const user=await getChatGPTUser();if(!user)return reply({error:'Нужно войти в аккаунт.'},401);if(request.headers.get('sec-fetch-site')==='cross-site')return reply({error:'Запрос отклонён.'},403);if(!request.headers.get('content-type')?.startsWith('application/json'))return reply({error:'Нужен JSON.'},415);if(Number(request.headers.get('content-length')||0)>900000)return reply({error:'Слишком большой объём данных.'},413);const text=await request.text();if(text.length>800000)return reply({error:'Лимит данных превышен. Выгрузите историю и удалите старые записи.'},413);let payload;try{payload=JSON.parse(text)}catch{return reply({error:'Некорректный запрос.'},400)}const parsed=stateSchema.safeParse(payload.state);if(!parsed.success||!Number.isSafeInteger(payload.revision)||payload.revision<0)return reply({error:parsed.success?'Некорректная версия.':parsed.error.issues[0].message},400);const data=JSON.stringify(parsed.data),now=new Date().toISOString(),db=database();const result=payload.revision===0?await db.prepare('INSERT OR IGNORE INTO workspaces (user_id,data,revision,updated_at) VALUES (?,?,1,?)').bind(user.userId,data,now).run():await db.prepare('UPDATE workspaces SET data = ?,revision = revision + 1,updated_at = ? WHERE user_id = ? AND revision = ?').bind(data,now,user.userId,payload.revision).run();if(!result.meta.changes)return reply({error:'Данные изменились в другой вкладке. Скопируйте несохранённый текст и обновите страницу.'},409);return reply({revision:payload.revision+1})}catch{console.error('JobPilot state write failed');return reply({error:'Не удалось сохранить. Ваш ввод остался на экране — повторите попытку.'},503)}}
